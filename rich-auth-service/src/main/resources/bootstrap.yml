logging:
  level:
    org.springframework.security: debug
spring:
  application:
    name: rich-auth-service
  mvc:
    static-path-pattern: /static/**
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        service-id: rich-config-service
      profile: ${spring.profiles.active}
      label: ${spring.profiles.active}
      fail-fast: true
    bus:
      trace:
        enabled: true
      enabled: true
third-party:
  wechat:
    app-id: wxb58205906d91941e
    app-secret: 0d8d3f95693970f8ec6c84a2c767b619
---
spring:
  profiles: dev
eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    service-url:
      defaultZone: http://127.0.0.1:10151/eureka
---
spring:
  profiles: prd
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:10151/eureka